import React from 'react'
import {baseUrl} from "@/utils/fakeUrl";

function AbautBanner() {

    return (
        <div className="abaut-banner">
            <img src={`${baseUrl}/img/image 5 (2).png`} alt={"abaut"} />
        </div>
    )
}

export default AbautBanner